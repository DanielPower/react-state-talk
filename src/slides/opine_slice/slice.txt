const webFlixSlice = {
  initialState: {
    favoriteMovies: [],
  },
  actions: {
    addMovieToFavourites: (set) => (movie) =>
      set(({ favouritemovies }) => {
        favouriteMovies.push(movie);
      }),
  },
};
